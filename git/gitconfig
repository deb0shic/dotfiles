[user]
	name  = deb0shic
	email = 97958510+deb0shic@users.noreply.github.com

[core]
	editor          = nvim
	autocrlf        = input
	excludesfile    = ~/.gitignore_global
	pager           = delta

[init]
	defaultBranch = main

[pull]
	rebase = true

[rerere]
	enabled = true

[rebase]
	autoStash = true

[merge]
	conflictStyle = zdiff3

[diff]
	algorithm = histogram
	colorMoved = default

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate  = true
	line-numbers = true
	syntax-theme = ansi

[alias]
	s      = status -sb
	co     = checkout
	br     = branch
	ci     = commit
	lg     = log --oneline --graph --decorate --all -30
	undo   = reset --soft HEAD~1
	amend  = commit --amend --no-edit
	wip    = "!git add -A && git commit -m 'wip'"
	root   = rev-parse --show-toplevel
	nuke   = "!git clean -fd && git reset --hard HEAD"

[push]
	default    = current
	autoSetupRemote = true

[fetch]
	prune = true
